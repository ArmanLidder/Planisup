{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "outDir": "./out-tsc/spec",
        "types": ["node", "mocha", "chai", "sinon"]
    },
    "include": [
        "app/**/*.spec.ts",
        "app/**/*.ts"
    ],
    "exclude": [
        "node_modules",
        "out"
    ]
}
